# Schedule Class
#
# Instanced for all Actor(renpy.store.object) objects

init -2 python:
    import re

    class Schedule(renpy.store.object):
            """ """
            def __init__(self):
                pass